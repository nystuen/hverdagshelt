/*
module.exports = function (app: Object, sendMail: Object) {

    app.post("/SendVerificationMail",(req,res)=>{
      console.log("sending verification mail");
      sendMail.sendMessage(req.body,(status,data)=>{
          res.status(status);
          res.json(data);
        });

    });

};*/